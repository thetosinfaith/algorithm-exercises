"""
Write a function called divide_or_square that takes one argument(a number) and
return the square root of the number if it's divisible by 5, return it's remainder
if it's not divisible by 5.

For example, if you pass 10 as an argument, then your function should return 3.16 as
the squareroot.

"""


def divide_or_square(number):
    number % 5
    for i in number:
        if number >= 5:
            return number
